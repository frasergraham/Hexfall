node_modules
dist
.vite
.DS_Store
*.log
# Sound source files (downloaded from freesound.org / kenney.nl).
# The encoded mp3s in src/assets/audio/ are the canonical assets;
# WAVs stay local for re-encoding but are not committed.
*.wav
ios/build
.env.local
.env.*.local
coverage/
docs/
# Server-to-server credentials for scripts/moderator.mjs. Never commit.
*.pem
moderator-token*.json
